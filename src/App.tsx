import React from 'react';
import './App.css';
import Input from './components/Input';
import Button from './components/Button';

function App() {
  return (
   <>
       <Input value={'...'} type={'text'} id={'b34234'} className={''} description={'Password'}/>
       <Button id={'a342134'} className={''} buttonName={'Register'}/>
   </>
  );
}

export default App;
