import {createSlice} from '@reduxjs/toolkit';

interface authSlice {

}

const authSlice = createSlice({
    name: 'autorization',
    initialState: {
      isAuthorized: false
    },
    reducers: {
    
    }
});
